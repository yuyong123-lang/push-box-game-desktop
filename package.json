{
  "name": "push-box-game-desktop",
  "version": "1.0.0",
  "description": "Push Box Game - Desktop Edition",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently -k \"cross-env VITE_DEV_SERVER_URL=http://localhost:3000 vite\" \"wait-on http://localhost:3000 && cross-env VITE_DEV_SERVER_URL=http://localhost:3000 electron .\"",
    "build": "vite build",
    "preview": "vite build && electron .",
    "electron:build": "vite build && electron-builder --win"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vueuse/motion": "^3.0.3",
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "vite": "^7.1.5",
    "wait-on": "^8.0.0"
  }
}
